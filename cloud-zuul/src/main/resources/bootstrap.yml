eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/
spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: cloud-discovery
        enabled: true
      profile: ${spring.profiles.active}
