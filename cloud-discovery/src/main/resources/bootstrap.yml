eureka:
  client:
    service-url:
      defaultZone: http://peer1:8071/eureka/,http://peer2:8061/eureka/
spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config